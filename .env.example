# Frontend API URL — must include the /api/v1 prefix
# Local development:
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Production (set this in Vercel Environment Variables):
# NEXT_PUBLIC_API_URL=https://fdpac-backend-3acw.onrender.com/api/v1
